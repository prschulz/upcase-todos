<h3>Todo List:</h3>

<ul class="todos">
	<% @todos.each do |todo| %>
		<li class="<%= "completed" if todo.completed? %>">
			<%= todo.title %> - <%= button_to "Complete", todo_completion_path(todo) %>
		</li>
	<% end %>
</ul>

<%= link_to "Add a new todo", new_todo_path %>
